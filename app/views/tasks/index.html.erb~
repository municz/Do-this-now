<h1>Listing tasks</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th>Priority</th>
    <th>Title</th>
    <th>Done</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tasks.each do |task| %>
  <tr <%= raw('class="done"') if task.done? %>>
    <td><%= link_to "[UP]", move_up_url(:id => task.id) %></td>
    <td><%= link_to "[DOWN]", task %></td>
    <td><%= task.priority %></td>
    <td><%= task.title %></td>
    <td><%= task.done? ? "[YES]" : "[NO]" %></td>
    <td><%= link_to 'Show', task %></td>
    <td><%= link_to 'Edit', edit_task_path(task) %></td>
    <td><%= link_to 'Destroy', task, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Task', new_task_path %>
